# Copy to .env and replace blank credentials before starting the project.
TZ=Etc/UTC
IMMICH_VERSION=release
IMMICH_BIND_ADDRESS=0.0.0.0
IMMICH_PORT=2283

UPLOAD_LOCATION=../../runtime/immich/library
DB_DATA_LOCATION=../../runtime/immich/postgres

DB_PASSWORD=
DB_USERNAME=immich
DB_DATABASE_NAME=immich
